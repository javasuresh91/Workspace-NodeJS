/* Global Objects  These objects are modules, functions, strings and object
etc. Some of these objects aren't actually in the global scope but in the
module scope */

/* __dirname and __filename => It is a Global String */
console.log("\nDirectory whre the code is placed : "+__dirname);
console.log("Filename : "+__filename);

/** Timer function are the global function, we can see later in the learning(Timer.js)*/
